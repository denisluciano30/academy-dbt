
version: 2

models:
  - name: dim_clientes
    description: Tabela de dimensão de produtos
    columns:
      - name: cliente_id
        description: Identificador do cliente
        tests:
          - not_null   